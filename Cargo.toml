[package]
name = "minimal-rust"
version = "0.1.0"
edition = "2021"

# To use static binaries for Alpine/ARM64/etc, we keep this simple
[[bin]]
name = "hello_world"
path = "src/main.rs"

[profile.release]
opt-level = 's'
lto = true
